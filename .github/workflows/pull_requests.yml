name: SNT malaria pull requests

on:
  pull_request:
    types: [ opened, synchronize, ready_for_review, edited ]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  packages: read

jobs:
  config:
    uses: BLSQ/iaso-ci/.github/workflows/wc_config.yml@IA-5227-decouple-ci-actions-into-dedicated-repo
    permissions:
      pull-requests: write
      contents: read
    with:
      branch: ${{ github.head_ref || github.ref_name }}
      pr_title: ${{ github.event.pull_request.title }}

  build_and_push_image:
    needs: config
    uses: BLSQ/iaso-ci/.github/workflows/wc_docker_build.yml@IA-5227-decouple-ci-actions-into-dedicated-repo
    with:
      branch: ${{ github.head_ref || github.ref_name }}
      image_tags: ${{ needs.config.outputs.image_tags }}
    secrets: inherit

  tests:
    uses: BLSQ/iaso-ci/.github/workflows/wc_tests.yml@IA-5227-decouple-ci-actions-into-dedicated-repo
    with:
      branch: ${{ github.head_ref || github.ref_name }}
